<UserControl x:Class="ERP.UI.Views.BeschäftigtView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ERP.UI.Views"
             xmlns:viewmodels="clr-namespace:ERP.UI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             Background="Transparent"
           
         >


    <UserControl.Resources>
        <!-- Fade-in animation -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="OverlayGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5"
                             FillBehavior="HoldEnd"/>
        </Storyboard>

        <!-- Fade-out animation -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="OverlayGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.5"
                             FillBehavior="Stop"/>
        </Storyboard>

        <!-- Pulsing glow animation -->
        <Storyboard x:Key="GlowPulseStoryboard" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="GlowEllipse"
                             Storyboard.TargetProperty="Opacity"
                             From="0.2" To="0.8" Duration="0:0:1.5" />
            <DoubleAnimation Storyboard.TargetName="GlowEllipse"
                             Storyboard.TargetProperty="Width"
                             From="130" To="160" Duration="0:0:1.5" />
            <DoubleAnimation Storyboard.TargetName="GlowEllipse"
                             Storyboard.TargetProperty="Height"
                             From="130" To="160" Duration="0:0:1.5" />
        </Storyboard>
    </UserControl.Resources>

    <!-- Основна структура -->
    <Grid x:Name="OverlayGrid" Background="#AA000000" Opacity="0">

        <Grid.Triggers>
            <!-- Автоматско стартување на светлосниот ефект -->
            <EventTrigger RoutedEvent="Grid.Loaded">
                <BeginStoryboard Storyboard="{StaticResource GlowPulseStoryboard}" />
            </EventTrigger>
        </Grid.Triggers>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >

            <!-- Светлосен круг зад спинерот -->
            <Ellipse x:Name="GlowEllipse"
                     Width="140" Height="140"
                     Fill="DodgerBlue"
                     Opacity="0.3"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     RenderTransformOrigin="0.5,0.5">
                <Ellipse.Effect>
                    <BlurEffect Radius="40"/>
                </Ellipse.Effect>
            </Ellipse>

            <!-- Спинерот -->
            <Control Width="100" Height="100"
                     Style="{StaticResource SpinnerStyle}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Margin="0,20,0,0"/>

            <!-- Текст -->
            <TextBlock Text="Anmeldung läuft..."
                       Foreground="Green"
                       FontSize="20"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>

    <!-- Fade-in/out тригери -->
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
        </EventTrigger>
        <EventTrigger RoutedEvent="Unloaded">
            <BeginStoryboard Storyboard="{StaticResource FadeOutStoryboard}"/>
        </EventTrigger>
    </UserControl.Triggers>

</UserControl>
